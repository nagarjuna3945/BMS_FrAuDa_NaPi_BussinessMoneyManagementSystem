import { OnlyDecimalPartPipePipe } from './only-decimal-part-pipe.pipe';

describe('OnlyDecimalPartPipePipe', () => {
  it('create an instance', () => {
    const pipe = new OnlyDecimalPartPipePipe();
    expect(pipe).toBeTruthy();
  });
});
